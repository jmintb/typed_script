
fn main() {
  let test = "222"
  if (1) < (0) {
    move_val(test)  
  } else {
    print(test , 3)
  }

  borrow_val(test)
  
  return; 
}

fn move_val(owned test: str) {
  return;
}

fn borrow_val(test: str) {
  return;
}

fn print(owned val: str, len: integer) {
  return;
}
