fn main() {
  let i = 0;
  while (i) < (2) {
     let test = "test" 
    borrow_val(test)
    i = (i) + 1;

  }

  borrow_val(test)
  return;
}

fn move_val(owned test: str) {
  return; 
}

fn borrow_val(test: str) {
  return; 
}
