fn main() {
  let val = "test";   
  let i = 0;
  let values = ["hello\n", "world\n"];


  while (i) < (2) {
    borrow_val(val);
    print(values[i], 6);
    i = (i) + (1);
  }

  return;
}

fn borrow_val(v: str) {
  return; 
}
