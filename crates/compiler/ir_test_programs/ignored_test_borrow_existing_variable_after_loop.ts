fn main() {
  let test = "test";
  let i = 0;
  while (i) < (2) {

    if (1) > (0) {
      print(test)
    } else {
      borrow_val(test)
    }
    
    borrow_vall(test)
    i = (i) + (1);
  }

  borrow_valll(test)
  return;
}

fn move_val(owned test: str) {
  return; 
}

fn borrow_val(test: str) {
  return; 
}

fn borrow_vall(test: str) {
 return; 
}

fn borrow_valll(test: str) {
  return; 
}
